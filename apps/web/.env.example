# apps/web/.env  — copy to apps/web/.env and fill in. Vite exposes only VITE_* to the client.
# The publishable key is browser-safe (it replaces the old anon key).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=http://localhost:4000
